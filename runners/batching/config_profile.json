{
    "runner": {
        "#": "Version, if present, must be 1",
        "version": 1
    },
    "configurations": [
        {
            "batches": 1,
            "duration": 120,
            "warmup": 0,
            "ruby": ["2.6.2"],
            "framework": ["rails"],
            "rack_env": "profile",
            "debug_server": true,
            "#extra_env": [ {}, { "RUBYOPT": "--jit" } ]
        }
    ]
}
